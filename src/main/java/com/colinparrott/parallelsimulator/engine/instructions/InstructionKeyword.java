package com.colinparrott.parallelsimulator.engine.instructions;

/**
 * List of keywords for each instruction
 */

public enum InstructionKeyword
{
    LD, ST, LDI, ADD, ADDI, SUBI, MUL, MULI, DIV, DIVI, BEQ, BNE, BGT, JUMP
}
